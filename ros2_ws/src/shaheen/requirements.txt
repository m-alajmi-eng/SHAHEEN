# Python dependencies for the SHAHEEN nodes (install with pip).
# ROS 2 packages (rclpy, cv_bridge, sensor_msgs, std_srvs, tf2_ros,
# visualization_msgs, ros_gz_bridge) come from apt / rosdep.
ultralytics>=8.0.0   # YOLOv8n perception
mavsdk>=1.4.0        # PX4 flight control (mission supervisor)
